fastapi==0.104.1
uvicorn[standard]==0.24.0
sqlalchemy==2.0.23
pydantic==2.5.0
pydantic-settings==2.1.0
pydantic[email]==2.5.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6
python-dateutil==2.8.2

# Cloudflare R2 Storage (optional, only needed if EXPMS_STORAGE_TYPE=r2) -
# R2 is S3-compatible, so the standard AWS SDK talks to it via a custom endpoint.
boto3==1.34.162
